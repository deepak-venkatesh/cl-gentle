#|

ZEROP will generate a T or an NIL. ADD1 needs a numeric input and not a T or NIL.

The EQUAL needs two inputs but it is receiving only one input.

NOT can receive only one input but it is receiving two inputs in this case.


|#
